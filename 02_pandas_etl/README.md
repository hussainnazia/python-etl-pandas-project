# Pandas ETL Sales Pipeline

A beginner Data Engineering ETL pipeline using Python and Pandas.

## Pipeline

CSV → Extract → Clean → Transform → Analyze → Load

## Skills Practiced

- Pandas
- Data cleaning
- `pd.to_numeric()`
- `dropna()`
- Calculated columns
- `groupby()`
- `agg()`
- Filtering
- `to_csv()`
- Python functions
